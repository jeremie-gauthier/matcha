{
	"name": "api-matcha",
	"version": "1.0.0",
	"description": "API for matcha 42's project",
	"main": "index.ts",
	"scripts": {
		"start": "nodemon src/index.ts",
		"build": "npx tsc",
		"clean": "rm -rf dist",
		"re": "rm -rf dist node_modules yarn.lock && yarn install"
	},
	"license": "UNLICENSED",
	"dependencies": {
		"express": "^4.17.1"
	},
	"devDependencies": {
		"@types/express": "^4.17.4",
		"@types/node": "^13.11.1",
		"nodemon": "^2.0.2",
		"ts-node": "^8.8.2",
		"tslint": "^6.1.1",
		"typescript": "^3.8.3"
	}
}
